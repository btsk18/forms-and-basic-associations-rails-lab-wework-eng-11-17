<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.collection_select :genre, Song.all, :id, :name %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>
